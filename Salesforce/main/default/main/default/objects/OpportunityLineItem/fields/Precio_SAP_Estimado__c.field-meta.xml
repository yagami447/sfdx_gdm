<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Precio_SAP_Estimado__c</fullName>
    <externalId>false</externalId>
    <formula>IF(Opportunity.CloseDate &lt; DATE(2015, 11, 24),
(ROUND(ROUND((UnitPrice * Tipo_de_cambio__c),2) - (ROUND((UnitPrice * Tipo_de_cambio__c),2) * (Tasas__c * IF( ISPICKVAL(Opportunity.Account.Grupo_de_Cuentas__c, &apos;CMEX&apos;) , 0, 1)) / 100),2))
,
(UnitPrice * Tipo_de_cambio__c - (UnitPrice * Tipo_de_cambio__c * (Tasas__c * IF( ISPICKVAL(Opportunity.Account.Grupo_de_Cuentas__c, &apos;CMEX&apos;) , 0, 1)) / 100))
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Preço Liquido SAP</label>
    <precision>18</precision>
    <required>false</required>
    <scale>6</scale>
    <type>Currency</type>
</CustomField>
