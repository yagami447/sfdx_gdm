<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Precio_venta_descuento__c</fullName>
    <externalId>false</externalId>
    <formula>IF(OR(ISNULL(Descuento_porc__c), Descuento_porc__c = 0), IF(OR(ISNULL(Descuento__c), Descuento__c = 0), 0, UnitPrice - Descuento__c), UnitPrice - (Descuento_porc__c * UnitPrice / 100))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Preço venta c / descuento</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <type>Currency</type>
</CustomField>
